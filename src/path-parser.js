function makePath(watcherPath) {
    var path = {},                                                  // Path final
        slicedFolders = [];                                         // Dossiers et sous-dossiers récupérés
    let i=0;
    for (const path of Object.keys(watcherPath)) {
        if (i==0) continue;
        
        i++;
    }
}